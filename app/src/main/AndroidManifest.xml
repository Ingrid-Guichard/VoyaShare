<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.iguichar.voyashare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Chargement">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PageGarde" />
        <activity android:name=".Pageconnexion" />
        <activity android:name=".PageAccueil" />
        <activity android:name=".ProposerTrajet" />
        <activity android:name=".RechercherTrajet" />
        <activity android:name=".Inscription" />
        <activity android:name=".ModifierProfil" />
        <activity android:name=".ListeTrajets"></activity>
    </application>

</manifest>